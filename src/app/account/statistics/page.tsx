import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Statistics | Doggy Net",
};

export default async function StatisticPage() {
  return (
    <main>
      <h1>Statistics</h1>
    </main>
  );
}
